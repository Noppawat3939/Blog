import { STYLES } from '../../constants'

export const Styles = {
    container: {
        height: '100vh',
    },
    noBlogContainer: {
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center',
        height: '100%',
    },
    noBlogTitle: {
        color: STYLES.COLORS.DARK,
    },
    noBlogBtn: {
        borderRadius: '20px',
        fontSize: 13,
        marginTop: '20px',
    },
}
